import json

class Envelope:
  print("******************************")

  name = []
  get_name = input("Enter name: ")
  get_amount = input("Enter amount: $ ")
  get_description = input("Enter description: ")  
  get_envelope = input("Enter account: ")

def __init__(self, name = ""):
  self.budget = budget

  def ledger(name, amount = "0", description = "", current_balance = "0"):
    def __str__(self):
      output = self.budget.center (40, "*")
      for item in self.ledger:
        output += '\n' + item['description'][:32].ljust(32) + f"{item['amount']:.2f}".rjust(8)
        output += '\n' + f"Total: {round(self.current_balance, 2)}"
    return output
  return ledger()    






def enter_transaction = 

with open(transaction.txt', 'w') as json_file:
  json.dump(budget, json_file)


